// Константы (цвета, стили)

import 'package:flutter/material.dart';

class AppConstants {
  static const primaryColor = Colors.blueAccent;
  static const secondaryColor = Colors.white;
  static const fontFamily = 'Roboto';
}
